Single repetitions contain ~1e6 entries, which is too huge to save. Thus, only averaged C(A) are saved.

Model changes are contained in the utils.py file, in the function of get_spetiation time. Lines of different changes are saved in comments.

The data can be computed with the notebook plot_CA.
